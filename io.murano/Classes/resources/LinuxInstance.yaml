Namespaces:
  =: io.murano.resources
  std: io.murano

Name: LinuxInstance

Extends: Instance

Methods:
  createDefaultInstanceSecurityGroupRules:
    Arguments:
      - groupName:
          Contract: $.string().notNull()
    Body:
      - $cloud: $.getCloud().require()
      - $rules:
        - ToPort: 22
          IpProtocol: tcp
          FromPort: 22
          External: true
      - $cloud.securityGroupManager.addGroupIngress(
          rules => $rules, groupName => $groupName)
